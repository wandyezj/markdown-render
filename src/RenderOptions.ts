export interface RenderOptions {
    /**
     * Handle output of a heading one
     */
    headingOne: (heading: string) => string;
    headingTwo: (heading: string) => string;
    headingThree: (heading: string) => string;
    headingFour: (heading: string) => string;
    headingFive: (heading: string) => string;

}
